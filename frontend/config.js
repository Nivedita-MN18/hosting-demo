/**
 * TraceLite Frontend Config
 * Edit this file to point at your deployed backend.
 * This is the only file you need to change for production.
 */
window.TRACELITE_CONFIG = {
    API_URL: "http://localhost:3001",
    API_KEY: "dev-secret-key",
};
